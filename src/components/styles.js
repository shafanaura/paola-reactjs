import { makeStyles } from "@material-ui/core/styles";

const styles = makeStyles((theme) => ({
	bgimage: {
		backgroundImage: "../assets/img/bg.png",
		backgroundColor: "#192256",
	},
	cont: {
		height: 50,
		backgroundColor: "#192250",
	},
}));

export default styles;
